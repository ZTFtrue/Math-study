import { MouseWheelDirective } from './mouse-wheel.directive';

describe('MouseWheelDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseWheelDirective();
    expect(directive).toBeTruthy();
  });
});
